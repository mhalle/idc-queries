title: Get all volume measurements

queryReturns:
  - name: 
  - name: volume

query: |
  SELECT
    *
  FROM
    `bigquery-public-data.idc_current.quantitative_measurements`
  WHERE
    Quantity.CodeMeaning = "Volume"
